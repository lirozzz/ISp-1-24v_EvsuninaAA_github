# Практика по Github
## ИСп-1-24в Евсюнина АА
### Из прошлой работы я узнала следующие команды: *git config --global user.name "Имя" * git config --global user.email "email@example.com" * git init, git status * git add имя_файла * git reset*  git commit -m "сообщение" * git log * git log --oneline * git diff хэш


